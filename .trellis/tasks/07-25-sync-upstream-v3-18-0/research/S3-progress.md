# S3 progress log

Append-only. Format: `<hash-or-file> DONE|ADAPTED|EXCLUDED|GATE — <note>`

- START — created progress log before any code work. Branch sync/upstream-v3.18.0, HEAD f5ade089, clean tree (only .pi/ + S3-brief.md untracked).
- ffc22ea7 ADAPTED — resolved AddProviderDialog semantically: retained fork's non-Claude-Desktop app surface and failure handling, added upstream save-then-sync flow with success/warning toasts, removed obsolete addSuccess key; zh-TW remains excluded because the fork deleted Claude Desktop scope.
- e191af4a ADAPTED — OpenCode live import now updates existing provider settings while preserving the stored display name when live omits one; upstream startup log/comment changes moved from obsolete desktop-only lib.rs block into shared tauri-free bootstrap.rs so desktop/web stay in parity. Focused regression test PASS (1/1).
- e78aa8a7 ADAPTED — OpenClaw and Hermes live imports now update existing DB providers and shared bootstrap logs sync outcomes. Preserved fork Hermes protections by stripping `_cc_source`/`provider_key` before both comparison and persistence, and reused the fork's existing Hermes test helper instead of duplicating it. Focused update tests PASS (3/3); rustfmt check PASS.
- 8b1ce764 ADAPTED (CONVERGENT) — fork already failed closed and used managed atomic writes from its MCP audit. Kept that stronger implementation and existing remove-path regression, added upstream rationale and richer single-sync assertion that the error names config.toml and leaves bytes untouched. Focused test PASS (1/1).
- 93f56198 ADAPTED (CONVERGENT) — fork's authoritative MCP projection audit had already implemented strip of `[mcp_servers]` plus legacy `[mcp.servers]` during Codex backfill with byte-identical no-op behavior and integration coverage. Kept fork implementation/tests, added upstream orphan-resurrection rationale, and excluded unrelated unified-session/model-catalog conflict context. Focused tests PASS (3/3).
- 473c2aaa ADAPTED (CONVERGENT) — fork already stripped Codex routing/MCP/credential artifacts. Preserved its stronger rule that only cc-switch's generated `model_catalog_json` path is removed (user custom catalog paths remain shareable), retained S2/provider strip behavior, and ported richer rationale/assertions for MCP, wire_api, bearer token, catalog sentinel, and web_search sentinel. Focused extraction tests PASS (3/3). Claude generic credential scrubbing remains deliberately deferred per brief.
- 6d2ee247 EXCLUDED (trigger) / ADAPTED (mechanism) — upstream hook is exclusively for the unported unified Codex-session toggle, so reapply_current_codex_official_live, re-export, and its official-routing tests were not added. The fork already has the required targeted `sync_enabled_for_app` mechanism plus stronger complete Codex-table replacement, empty-set orphan clearing, fail-closed TOML, and cross-app aggregate sync; added a generic contract comment. Focused Codex projection tests PASS (3/3).
- 1f36f0cf ADAPTED — ported switch-away live common-config re-extraction before value-matched backfill plus both Codex end-to-end tests (new shared keys/secret-artifact isolation and deletion propagation). Fork scope is intentionally Codex-only: upstream's pre-existing Claude autosync prerequisite was absent here, and enabling it against this fork's known-unscrubbed OPENROUTER/GOOGLE/OPENAI/GEMINI/AWS credentials would create automatic cross-provider secret propagation. Focused Codex switch tests PASS (2/2).
- 11c173c7 ADAPTED (CONVERGENT) — fork's MCP atomicity audit already had best-effort aggregate all-app projection, target-only save/switch projection, target-only sync-current behavior, deferred aggregate error after Skill sync, complete Codex replacement, and non-fatal post-write projection. Ported fuller contracts and the missing switch-vs-broken-Claude regression; reused the existing aggregate regression instead of duplicating it. Focused tests PASS (2/2).
- 94fc1cc0 ADAPTED — all five app importers still run best-effort, but any per-app failure now returns an aggregate error containing the already-persisted count and failed app(s); partial successes remain in DB. Kept the fork's importer-function table, ported the upstream partial-failure regression, updated prior all/partial-failure tests, and changed React Query invalidation to onSettled so error responses still refresh imported rows. Focused tests PASS (3/3); command/web handler already delegated to the shared service.
- 88d5ffba ADAPTED (CONVERGENT) — fork already ported the whole substance during its C5/atomicity audit: backend `update_toml_common_config_snippet` (toml_edit) in live.rs + command + lib.rs registration + mod.rs re-export + fork-only web handler/route (`POST /api/config/update-toml-common-config-snippet`), FE smol-toml helper deleted from providerConfigUtils.ts, backend-invoking configApi wrapper, async modal/editor signatures, and a hook that is a strict SUPERSET of upstream's guards (tomlOpSeq + config-baseline like upstream, PLUS serialized save queue, preset-identity invalidation, unmount invalidation). Both upstream FE race regressions exist under fork titles in useCommonConfigSave.test.tsx (plus 3 fork-only ones). Only delta ported: upstream's richer backend test assertions — the no-synthesized-`[model_providers]`-header C5 lock, merged-value + survivor-key assertions, assertion messages, and the scalar-override/value-matched-removal rationale comment. Focused tests PASS (2/2).
- 6245caa6 ADAPTED — OpenCode known field editors (model limit editor, headers editor, collapsible extra-options, draft-prefix rename `option-`→`draft-option:`/`draft-header:` so legitimate `option-*`/`header-*` keys survive). Cherry-pick auto-merged everything except zh-TW.json (modify/delete — resolved by keeping it DELETED per fork scope); the fork's local inline ModelDropdown (never extracted to ./shared here) survived untouched. Both upstream test files taken verbatim (props/type parity verified: `OpenCodeModel.limit`, `OpenCodeProviderOptions.headers`, ui/collapsible all pre-exist; single OpenCodeFormFields call site). Focused tests PASS (14/14); tsc PASS; prettier PASS on all touched files.
- GATE — `cargo fmt --check` PASS after applying fmt to 2 files with drift from earlier S3 items (`services/provider/mod.rs` assertion wrap, `tests/import_export_sync.rs` blank line); fmt-only diffs staged.
- GATE — `cargo clippy --all-targets -- -D warnings` (desktop) PASS.
- GATE — web `cargo clippy --no-default-features --features web-server --example server -- -D warnings -A dead_code -A clippy::duplicate_mod` PASS; web `cargo check` (same features) PASS.
- GATE — `cargo test --lib` UNFILTERED: **1599 passed / 0 failed / 2 ignored**.
- GATE — `npx tsc --noEmit` PASS; `npm run format:check` PASS; `check-web-route-coverage` commands 267 / missing 0 / methodMismatch 0 / parityFallback 0; `check-locale-parity` in parity; `npx vitest run` **124 files / 669 tests PASS** (+2 files/+14 tests = the two new OpenCode suites).
- END STATE — `git ls-files -u` empty; no CHERRY_PICK_HEAD/MERGE_HEAD/sequencer; zero conflict markers in staged content; `git diff --check` clean; all work staged, NO commit made.
