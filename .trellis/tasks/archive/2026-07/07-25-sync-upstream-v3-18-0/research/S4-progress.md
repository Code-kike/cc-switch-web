# S4 progress log — Project Profiles

Append-only checkpoint log. Format: `<hash-or-file> DONE|ADAPTED|EXCLUDED|GATE — <note>`

- START — created progress log before any code work. About to read S4-brief.md, S2-report.md, S3-report.md and verify git status.
- RECON DONE — verified: profile feature files at v3.18.0 are byte-identical to their post-44279987 state (only tray.rs/App.tsx have later non-profile changes from S5+). Strategy: build the adapted final state directly (Claude+Codex scopes only), attribute per-commit in the report; items 6 (proxy.rs) and 14 (health-check) handled as targeted ports. Upstream sources read: services/profile.rs, commands/profile.rs, dao/profiles.rs, schema/dao/proxy/tray/settings hunks, profile_roundtrip.rs (814 lines), FE profiles/* + wiring diffs + i18n keys, 06039540 full diff.
- RECON-2 DONE — key adaptations mapped: SCHEMA_VERSION 13→14 (fork diverged, profiles needs v14 not upstream's v12); ProfileScope={Claude,Codex} drop ClaudeDesktop; web build includes database/mod.rs whole (dao/profiles.rs auto-included) but services enumerated (needs web_services.rs profile shim + SkillService re-export); commands/* are desktop-only (no web compile); FE must use adapter invoke not @tauri-apps/api/core; App.tsx uses listen() pattern not useTauriEvent; item-14 stream_check_confirmed is ACTIVELY used by fork ProviderList (upstream removed the whole confirm flow earlier) — PRESERVE it, only port testConfig removal + module rename. Web apply handler wraps ProfileService::apply in spawn_blocking (internal futures block_on + many switches).
- BACKEND-CORE ADAPTED — completed v14 profiles schema/migration + canonical SQL restore allowlist, DAO/service/command exports and desktop registration, web service shim + SkillService re-export, S2-compatible managed-atomic takeover disable, last-takeover detection, and Claude/Codex-only tray project submenus/apply flow; added migration/dev-marker regressions.
- BACKEND-CORE GATE — `cargo check --lib` PASS after profile core/proxy/tray wiring (initial root invocation was corrected to src-tauri; no code failure).
- WEB-PARITY ADAPTED — audited the fork-only Axum profiles router/handlers, command-manifest owner mapping, six `web-commands.ts` entries, handler/router wiring, and web service shim; removed unnecessary profile re-exports that failed `-D warnings` while retaining the required module + SkillService export. Web clippy PASS, generated manifest check PASS (273 commands), route coverage PASS (missing/methodMismatch/parityFallback all 0), and `git diff --check` PASS.
- FRONTEND-PROFILES ADAPTED — ported the final shared-project UI/API/query shape with Claude+Codex scopes only: adapter-based `profilesApi`, scope mirror/snapshot detection, switch/create/clear/manage flows, per-scope cache invalidation, runtime-neutral `profile-applied` listener (including takeover/proxy status), header placement, and `showProfileSwitcher` persisted through Rust/TS settings plus General-page toggle. Added retained-locale strings to en/ja/zh; Claude Desktop and zh-TW remain excluded.
- 06039540 ADAPTED — removed per-provider `meta.testConfig` end-to-end (Rust/TS types, stream-check merge, provider editor state/UI, stale locale keys), renamed `model-test.ts`→`connectivity-check.ts` and `ModelTestConfigPanel`→`ConnectivityCheckConfigPanel`, and renamed the Settings section. Preserved the fork's real streaming model/prompt global configuration, active `streamCheckConfirmed` first-run flow, guarded web dial path, and S2 official-OAuth batch skip instead of taking upstream's incompatible reachability-only context.
- TESTS ADAPTED — added the Claude/Codex-only upstream `profile_roundtrip` suite (6 tests; temp HOME via support.rs), profile service/DAO/migration/default-setting regressions, Web adapter method/encoding coverage, scope null-vs-empty semantics, ProfileSwitcher current-id/scope behavior, settings toggle persistence, connectivity-panel rename coverage, and App `profile-applied` takeover-cache invalidation. Focused Rust profile tests PASS (6 integration + 17 filtered lib); focused FE suite PASS (7 files / 34 tests).
- GATE — `cargo fmt --check` PASS (no output).
- GATE — `cargo clippy --all-targets -- -D warnings` PASS (`cc-switch v3.16.5`; finished dev profile in 51.22s).
- GATE — web `cargo clippy --no-default-features --features web-server --example server -- -D warnings -A dead_code -A clippy::duplicate_mod` PASS (finished dev profile in 25.35s).
- GATE — web `cargo check --no-default-features --features web-server --example server` PASS (64 expected dead-code warnings from the standalone shim; finished dev profile in 18.01s).
- GATE — unfiltered `cargo test --lib` PASS: 1613 passed / 0 failed / 2 ignored / 0 filtered; finished in 9.67s.
- GATE — `cargo test --test profile_roundtrip` PASS: 6 passed / 0 failed / 0 ignored; finished in 3.04s.
- GATE — `npx tsc --noEmit` PASS (no output).
- GATE — `npm run format:check` PASS: all matched `src/**/*.{js,jsx,ts,tsx,css,json}` files use Prettier style.
- GATE — `node scripts/check-web-route-coverage.mjs` PASS: commands 273, routes 261, wildcardRoutes 20, unsupported 29, webReplacements 7, webFetchLiteralPaths 4, missing 0, methodMismatch 0, danglingReplacementPaths 0, parityExact 5, parityFallback 0.
- GATE — `npm run check:locales` PASS: en/ja/zh each 2395 keys, totalUniqueKeys 2395, inParity true.
- GATE — `npx vitest run` PASS: 127 test files / 677 tests; duration 60.66s. Non-failing expected stderr from negative-path tests, existing MSW/browser-data/React/CodeMirror warnings only.
- GATE — final hygiene PASS (pre-report): `git diff --check` clean; zero conflict markers; `git ls-files -u` empty; CHERRY_PICK_HEAD/MERGE_HEAD/REVERT_HEAD/sequencer absent; no staged files; no changed zh-TW/Claude Desktop scope path or executable Claude Desktop profile scope; profile frontend has no direct Tauri API/global use and imports `invoke` only from `./adapter`.
- REPORT DONE — wrote `S4-report.md` with all 14 upstream outcomes, fork adaptations/non-port code, exact green gate matrix, deferrals, and final hygiene; recovered verbatim from the failed report worker's pending `write` payload after an unexpected EOF.
- CONTEXT ADAPTED — refreshed stale S3-only `implement.jsonl`/`check.jsonl` entries to the S4 brief/progress/report plus the overlapping S2/S3 reports and current specs, so resumed implementation/check agents receive the correct batch context.
- CHECK-BACKEND FIXED — independent Phase 2.2 review found `disable_takeover_for_app_sync` restored/cleared takeover state without the existing per-app switch lock, allowing a concurrent takeover/hot-switch race. It now holds that lock around restore → backup deletion → flag/health cleanup and calls the non-reentrant `_inner` restore; added `profile_takeover_disable_serializes_on_switch_lock`. PASS: new regression (1), profile integration (6), filtered profile/migration lib set (18), route coverage (273 commands; 0 missing/mismatch), `cargo fmt --check`, and `git diff --check`. The review worker timed out only after these commands completed.
- CHECK-FRONTEND FIXED — Phase 2.2 review found profile events invalidated a nonexistent `['prompts', scope]` React Query key while `PromptPanel` owns local hook state, so an open prompt view could stay stale after a profile apply from another Web client or the desktop tray. `PromptPanelHandle` now exposes `reload`, the runtime-neutral `profile-applied` listener reloads the matching active scope directly, the fake invalidations were removed, and an App integration regression covers matching vs non-matching scopes. Also updated retained connectivity-check locale copy that still named the removed Model Test Config surface.
- CHECK-FRONTEND GATE — focused frontend verification PASS: profile/App/connectivity suites 5 files / 31 tests; `npx tsc --noEmit`; `npm run format:check`; and locale parity (en/ja/zh 2395 keys each).
- CHECK-FINAL GATE — complete post-fix S4 light gate PASS: `cargo fmt --check`; desktop clippy `-D warnings`; Web clippy `-D warnings`; Web check (64 expected standalone-shim dead-code warnings); `cargo test --lib` 1614 passed / 2 ignored; profile roundtrip 6 passed; TypeScript; Prettier; route coverage 273 commands with zero missing/mismatch/fallback; locale parity 2395 keys each; full Vitest 127 files / 678 tests. Final hygiene clean: diff check, conflict markers, unmerged paths, sequencer state, staged state, profile frontend Tauri imports, and removed health-check symbol scan.
- SPEC UPDATED — added the executable `Project Profiles — Scoped Snapshot Apply and Runtime Refresh` contract to `frontend/quality-guidelines.md`, covering schema/API signatures, scope/null semantics, managed apply ordering, Web parity, mixed React Query/local-state refresh ownership, validation matrix, and required regressions.
