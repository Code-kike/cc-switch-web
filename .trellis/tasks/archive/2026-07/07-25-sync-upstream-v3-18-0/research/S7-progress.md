# S7 progress log — logging overhaul + misc fixes

Append-only checkpoint log. Format: `<hash-or-file> DONE|ADAPTED|EXCLUDED|GATE — <note>`

- START — recon of all 14 S7 commits complete (upstream diffs read, fork call
  sites verified); S7-brief.md written. Beginning implementation in
  topological order.
- 7e73a1ff DONE — proxyReasonAnthropicMessages added to en/ja/zh (key already referenced by useProviderActions.ts).
- f6e37ed9 ADAPTED — ported codex_state_db TOML literal-string test fix, settings.rs io::Write into cfg(unix), and the skill.rs CC_SWITCH_TEST_HOME regression test (fork already routes skill.rs through get_home_dir — convergent). CI matrix, codex_history_migration, anchored-upgrade misc tests, install_update_and_restart: N/A/excluded (absent in fork).
- edea624a DONE — init_default_skill_repos now one-shot via default_skill_repos_initialized flag; both upstream DB regressions added to database/tests.rs.
- 2bfecead DONE — kimi-k3 seed row (3.00/15.00/0.30).
- 6fddcaa9 DONE — bare k3 alias seed row.
- f2045822 ADAPTED — kimi-k3 appended to hermes/openclaw/opencode Kimi presets (fork base is kimi-k2.6, not k2.7-code); codexProviderPresets has no Kimi preset in this fork (chat-bridge-dependent) → that hunk N/A; codexChatProviderPresets.test.ts absent → skipped.
- e356fc6e ADAPTED — openclaw list-price repricing applied to all fork-present entries (deepseek-v4-pro cacheRead, glm-5.1 x2, qwen3.5-plus, kimi-for-coding 0.95/4/0.19, MiniMax-M2.7 x2 + ModelScope/SiliconFlow prefixed x2, KAT-Coder-Pro, Ling-2.5-1T, ZhipuAI/GLM-5.1, moonshotai/kimi-k2.5 0.6/3/0.1; kimi-k2.6 gains seed-aligned cacheRead 0.16). LongCat-Flash-Chat left as-is (upstream repriced LongCat-2.0, a model the fork never adopted — 7a8b9562 unported, out of S7 scope). Fork seed reconciled to keep L19 SSOT invariant: kimi-for-coding + ling-2.5-1t seed rows repriced + repair entries; kimi-k2.5 seed output aligned to upstream 3.00 + repair entry. openclawPresetPricing + opencode preset tests PASS (11/11).
- c4795e98 ADAPTED — fill_template_fields_from_static + parser-required whitelist ported onto the fork's un-split load_codex_model_catalog_template; both upstream tests added and PASS (fork gpt5_5_template.json already carries supports_reasoning_summaries).
- 22d2872c ADAPTED — new shared tauri-free src/logging.rs (RedactedUrl/url_for_log[_with_secrets]/redact_url_for_log[_with_secrets]/redact_url_origin_for_log + generic size rotation + frontend.log writer), #[path] shim in examples/server.rs; lib.rs: old redact fn removed, deeplink raw-debug dropped, plugin-log KeepSome(4)/20MiB, no startup delete, dispatch filter + runtime_log_level_allows (+test), early Info + banner + override replay, updater plugin after logger, DB log level applied right after DB open (fail-closed Info), late block removed; panic_hook crash.log 5MiBx2 rotation + lock; deeplink command/provider redacted; model_fetch known-secret redaction; forwarder log_secrets collection + target-for-log (origin-only w/o known secrets) + metadata-only body debug + hyper send_request log_display + without_url error mapping + CacheTrace endpoint redaction (summarize_upstream_body unchanged upstream-identical, fork test kept); hyper_client masked proxy log; handlers.rs Claude parse-failure now metadata-only (fork had 180-char compact — upstream stricter form adopted; chat/anthropic-bridge hunks + C7 body_snippet machinery N/A, bridge absent); response_processor body/SSE content omitted + format_headers allowlist + test; mcp/codex headers core-field + value-omitted logs + http_headers test; webdav redact delegates to shared helper. docs/user-manual N/A (absent). Focused tests 7/7 PASS; desktop lib + web example compile clean.
- 62747058 ADAPTED — frontendLogger.ts upstream-verbatim except writer: adapter invoke("log_frontend_error") (no @tauri-apps/plugin-log dep, no log:default capability); FrontendErrorBoundary.tsx replaces the fork's console-only ErrorBoundary (old component+test removed, renderCrash* keys dropped); main.tsx installs global handlers + reports config_load_error_listener/get_init_error; backend: shared logging::append_frontend_error (bounded 20k chars, 0600, 5MiBx2 rotation, mirrors log::error target=frontend), Tauri command commands/misc.rs::log_frontend_error, web POST /api/system/log_frontend_error, web-commands.ts entry, manifest regen (277 commands), route coverage missing 0/mismatch 0; i18n frontendCrash*/reloadInterface + logConfig + enableLogging copy in en/ja/zh (zh-TW stays deleted); tests: frontendLogger 18 + FrontendErrorBoundary 1 PASS.
- 9ca1a41f EXCLUDED — transform_codex_chat.rs absent (Codex Chat bridge never ported; S2 blocker cluster).
- 6d316c0b EXCLUDED — streaming_codex_chat.rs absent (same cluster).
- 613fef70 EXCLUDED — transform_codex_chat.rs absent (same cluster).
- 08710d51 EXCLUDED — transform_codex_chat.rs + transform_codex_anthropic.rs absent (same cluster); fork's own Claude-side clean_schema already carries S2 ded0b63a root-object injection.
- TESTS ADAPTED — added append_frontend_error disk-persistence regression (temp-HOME isolated, 0600 assertion) passing in BOTH the desktop lib suite and the web example suite, proving the Web runtime writes frontend errors to server-side disk.
- GATE — `cargo fmt --check` PASS.
- GATE — desktop `cargo clippy --all-targets -- -D warnings` PASS.
- GATE — web `cargo clippy --no-default-features --features web-server --example server -- -D warnings -A dead_code -A clippy::duplicate_mod` PASS.
- GATE — web `cargo check --no-default-features --features web-server --example server` PASS.
- GATE — `cargo test --lib` UNFILTERED PASS: 1732 passed / 0 failed / 2 ignored.
- GATE — web example `cargo test ... -- web_api:: dual_runtime_parity:: web_proxy_lifecycle::` PASS: 36 passed / 0 failed.
- GATE — `npx tsc --noEmit` PASS.
- GATE — `npm run format:check` PASS (new tests/ files also Prettier-clean).
- GATE — `npm run check:web-routes` PASS: commands 277, missing 0, methodMismatch 0, parityFallback 0.
- GATE — `npm run check:locales` PASS: en/ja/zh in parity (2442 unique keys).
- GATE — full `npx vitest run` PASS: 138 files / 741 tests.
- GATE — hygiene PASS: `git diff --check` clean, zero conflict markers, `git ls-files -u` empty; commands.manifest.json is git-ignored (regenerated locally to 277 commands incl. log_frontend_error).
- REPORT DONE — wrote S7-report.md: all 14 upstream outcomes, 22d2872c/62747058 adaptation detail, fork-seed reconciliation, non-port inventory, green gate matrix, deferrals.
- CHECK FIXED — independent Phase 2.2 review verified every ported hunk byte-for-byte against upstream (22d2872c shared files, 62747058 FE files, edea624a DAO+tests, c4795e98 core fn, f2045822/e356fc6e preset values) and found the e356fc6e seed reconciliation incomplete: `kat-coder-pro` seed still carried the old preset-derived 0.002/0.006 while the preset was repriced to 0.3/1.2/0.06 (150× runtime cost understatement), invisible to the L19 test because (a) its cost regex could not parse cacheRead/cacheWrite-bearing objects and (b) it compared preset ids case-sensitively while runtime lookup lowercases. Fixed: kat-coder-pro + peg-documented kat-coder-air seeds → 0.30/1.20/0.06 with repair entries (old 0.002/0.006 rows only); qwen3.5-plus seed cacheRead 0 → 0.052 + repair entry (upstream v3.16.5+ parity, preset carries 0.052 since this batch); L19 test regex tolerates extra cost fields, lowercases ids, and pins kat-coder-pro/minimax-m2.7/kimi-k3 sentinels against future parser vacuity.
- CHECK GATE — post-fix full gate PASS: `cargo fmt --check`; desktop clippy `-D warnings`; web clippy (`-D warnings -A dead_code -A clippy::duplicate_mod`) + web check (66 expected shim dead-code warnings); `cargo test --lib` 1732/0/2; web example `web_api:: dual_runtime_parity:: web_proxy_lifecycle::` 36/0 and `logging::` 6/0 (frontend.log disk write proven in the web build); `tsc --noEmit`; `format:check`; `check:web-routes` 277 commands / 0 missing / 0 mismatch; `check:locales` in parity; full vitest 138 files / 741 tests. Hygiene: `git diff --check` clean, no conflict markers, `git ls-files -u` empty. Exclusions re-verified: no sponsor/referral, no version bump, no workflow/updater-CI change (updater stays configless-disabled; only its registration moved after the logger per upstream), no auth surface, zh-TW deleted, no plugin-log npm dep/capability, no dangling ErrorBoundary/renderCrash refs.
