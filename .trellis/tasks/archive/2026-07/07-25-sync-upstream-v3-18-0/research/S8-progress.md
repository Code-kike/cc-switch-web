# S8 progress log — version 3.18.0 + changelog + full gate

Append-only checkpoint log. Format: `<unit> DONE|ADAPTED|GATE|SMOKE — <note>`

- START — created progress log. Working tree clean, S1–S7 committed (HEAD c5651b4e). About to recon version SSOTs, v3.16.5 bump precedent, and changelog convention.
- RECON DONE — v3.16.5 precedent (52197b9c): bump package.json + Cargo.toml + Cargo.lock (cc-switch entry) + tauri.conf.json, CHANGELOG.md summary + link, docs/release-notes/v<ver>-en.md (English only; ja/zh pruned by audit abfb6070). Rust/FE derive version from CARGO_PKG_VERSION//api/health; icons hit is SVG path noise. CHANGELOG [Unreleased] empty. SCHEMA_VERSION=16 confirmed. Read S1/S5/S6/S7 reports (S2/S3/S4 injected).
- VERSION-BUMP DONE — 3.16.5→3.18.0 in package.json, src-tauri/Cargo.toml, src-tauri/tauri.conf.json; Cargo.lock synced via `cargo update --workspace --offline` (only cc-switch entry changed; 117 deps untouched). Diff verified minimal (4 files, 4 lines).
- CHANGELOG DONE — wrote fork-own `docs/release-notes/v3.18.0-en.md` (per-theme S1–S7 summary, fork-side-since-3.16.5 section, Web adaptations, exclusions incl. no-auth/no-updater by design, schema v13→v16 upgrade notes) + CHANGELOG.md `[3.18.0] - 2026-07-31` summary entry with link, matching the 3.16.5 convention. [Unreleased] was empty (nothing to fold). Starting full gate suite.
- GATE — `cargo fmt --check` PASS.
- GATE — desktop `cargo clippy --all-targets -- -D warnings` PASS (compiled as cc-switch v3.18.0; 1m00s).
- GATE — web clippy `-D warnings -A dead_code -A clippy::duplicate_mod` PASS (33.11s); web `cargo check --example server` PASS (65 expected standalone-shim dead-code warnings).
- GATE — `cargo test --lib` PASS: 1732 passed / 0 failed / 2 ignored (10.10s).
- GATE — web example `web_api:: dual_runtime_parity:: web_proxy_lifecycle::` PASS: 36 passed / 0 failed.
- GATE — `pnpm format:check` PASS; `pnpm typecheck` PASS.
- GATE — `pnpm check:web-routes` PASS: commands 277, routes 265, missing 0, methodMismatch 0, parityFallback 0. `pnpm check:locales` PASS: en/ja/zh 2442 keys, inParity true.
- GATE — `bash scripts/gen-command-manifest.sh --check` PASS: manifest up to date (277 commands).
- GATE — `pnpm test:unit` (full vitest) PASS: 138 files / 741 tests (66.13s).
- GATE — `pnpm build:web` PASS (22.92s; pre-existing advisory chunk-size warning only).
- GATE — `pnpm test:integration` run 1: 7 files / 10 tests FAILED of 20 files / 50 tests — but my pipeline only captured the tail (WebdavSyncSection toast-timeout visible). Rerunning with full log capture to /tmp/s8-integration-full.log to separate known env-flakes (SessionManagerPage opencode.db leak, SkillsPage GitHub 404, ProviderList empty-state) from real regressions.
- SMOKE-PREP ADAPTED — added `log-frontend-error-persists` probe to scripts/smoke-web-server.mjs (POST /api/system/log_frontend_error with JSON body; asserts null result AND marker persisted in `${dataDir}/logs/frontend.log` — the S7 web disk sink). Added `dataDir` to smokeArtifacts. `node --check` OK. Will verify fresh-DB schema v16 via PRAGMA user_version after the smoke run.
- INTEGRATION-TRIAGE DONE — full-capture rerun reproduced run 1 identically (deterministic, not load flakes): 7 files / 10 tests failed. 3 = memory-known env/bootstrap flakes (SkillsPage×2 real-GitHub 404 + bootstrap-seeded repos; ProviderList empty-state import button gone due to F6 auto-import); SessionManagerPage PASSED (HOME isolation works). Root causes for the other 7 (all stale test expectations vs intentional S1–S7 behavior; test:integration was never run per-batch): (1) UsageDashboard×2 — S6 df3e07ed requires archived Codex filenames to end with a 36-char UUID; fixtures `page-usage-session*.jsonl` get deferred ("文件名缺少有效的尾部 UUID" confirmed in solo-run server log) → imported=0 → toast.info not success. (2) smoke-web-server.mjs same fixture-name issue + S6 c9ac6efd request-id format now `codex_session:thread-v1:<uuid>:<idx>`. (3) ProviderList Hermes balance — non-resolvable `unsupported-balance.example.com` now 400s at the outbound guard before the deterministic "Unknown balance provider" branch; S1 2df2212c makes usageApi.query REJECT → WebApiError thrown; fix = resolvable public host per the smoke F4 precedent. (4) BackupListSection — desktop-parity startup worker creates a baseline auto-backup on fresh dirs before serve (confirmed in solo log: backup at 11:37:49.998 vs listening 11:37:50); test expects [] initially. (5) ImportExportSection — SQL canonical restore now runs the longer v14/v15/v16 migration chain on the imported dump; restore lands ~1-2s after click (server log shows restore completed + settings fallback) but the test waitFor uses the 1s default. (6) WebdavSyncSection download-restore — same restore-latency class, same 1s default waitFor. (7) AuthCenterPanel — S5 updated webRemoteHintDescription copy to "Copilot / ChatGPT / xAI"; test regex pins the old copy. Fix plan: all test/fixture-side; no product code changes.
- INTEGRATION-FIXES ADAPTED — all 7 triaged failures fixed test/fixture-side, zero product changes: UsageDashboard fixtures renamed to trailing-UUID stems (+ distinct second UUID); smoke-web-server.mjs fixture renamed + `expectedRequestId = codex_session:thread-v1:<uuid>:1`; ProviderList Hermes balance → resolvable `https://example.com/v1` (F4 precedent comment); BackupListSection clears the startup baseline auto-backup via the real delete API before the flow; ImportExportSection + WebdavSyncSection restore waitFors get explicit 15s timeouts (canonical restore now replays the longer migration chain); AuthCenterPanel regex updated to the S5 "Copilot / ChatGPT / xAI" copy. Focused rerun: 5 files fully green, ProviderList 9/10 (only the known empty-state flake).
- GATE — `pnpm test:integration` (full, post-fix) — 20 files / 50 tests: **47 passed / 3 failed**; the 3 failures are exactly the memory-documented known env/bootstrap flakes (ProviderList empty-state import ×1; SkillsPage bootstrap-seeded repos + real-GitHub DOWNLOAD_FAILED ×2). Reported as env-flakes per task instruction; not chased.
- SMOKE-INFRA FIXED — first two `pnpm smoke:web-server` attempts timed out at startup: the smoke's HOME isolation (added for the opencode.db leak) pointed the rustup shim's `$HOME/.rustup` into the throwaway temp HOME, so cargo re-downloaded the pinned 1.95 toolchain every run (>300s) — the integration helper worked because it does NOT override HOME. Fixed by pinning `RUSTUP_HOME`/`CARGO_HOME` to the real home in the smoke spawn env; cleaned partial `.rustup` from the failed temp homes. Side effect of the fixture UUID rename: the archived usage fixture is now legitimately listed by the session manager, so the three session probes became subset-based (seeded ids present/absent) instead of exact-count.
- SMOKE PASS — `pnpm smoke:web-server` EXIT=0: **124 probes** (116×200, 6×501 expected desktop-only, 2×400 expected validation), incl. the new `log-frontend-error-persists` probe. E2E verified on the throwaway dataDir: `PRAGMA user_version = 16` on the fresh DB (v15→v16 migration line in startup log), `profiles` table present, `grokbuild` proxy row present, `logs/frontend.log` persisted the marker with mode 600, zero panic/fatal lines, clean SIGINT shutdown. Developer's real HOME/data untouched.
- GATE-RERUN (final toolchain state, after the rustup 1.95 channel sync) — `cargo fmt --check` PASS; desktop clippy `-D warnings` PASS (25.74s); web clippy PASS; web check PASS (65 expected shim warnings); `cargo test --lib` — one transient parallel-run failure on the first invocation, immediately green on unfiltered rerun (**1732 passed / 0 failed / 2 ignored**, 10.13s; same pre-existing flake class documented in S5: parallel scheduling timeout/$HOME interference); web example suites **36 passed**; manifest check PASS (277); `pnpm typecheck` PASS (tsconfig covers tests/**); `pnpm format:check` PASS; `check:web-routes` 277/0/0 PASS; `check:locales` parity PASS. vitest unit config excludes `**/*.web-server.test.tsx`, so the earlier 138 files / 741 tests unit result stands for my edits.
- HYGIENE — `git diff --check` clean; zero conflict markers; `git ls-files -u` empty; working tree contains exactly: version bump ×4, CHANGELOG.md, docs/release-notes/v3.18.0-en.md (new), scripts/smoke-web-server.mjs, 6 integration test files, S8-progress.md. No commit made. Untracked `.pi/`/`.pi-subagents/` pre-existing and untouched. No systemd restart, no merge, no auth, no updater, no zh-TW.
- DONE — S8 complete; final report returned to the dispatcher.
