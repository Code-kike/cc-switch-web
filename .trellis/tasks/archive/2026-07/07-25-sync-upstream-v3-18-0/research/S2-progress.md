# S2 progress log — Codex routing / protocol bridges

Append-only checkpoint log. Format: `<hash-or-file> DONE|ADAPTED|EXCLUDED — <note>`

Branch: `sync/upstream-v3.18.0`. Base at start: `903f2a56` (S1 committed as `e6390144`).

## Planned order (16 commits)

1. b3e5e32c — Claude subagent model config
2. 3538b392 — claude 1M checkbox on fallback model field
3. 95c917b3 — Zhipu team plan quota query
4. 99e11e08 — codex native Anthropic Messages protocol upstream
5. 50270d5e — exclude Fable model env from Claude common config (+ S1 deferral close)
6. ded0b63a — missing provider keys / tool schema types
7. c6197ae3 — single auth placeholder on managed Claude takeover
8. 7479d10d — codex default model field in provider form
9. 27ce0a51 — harden Responses reasoning + tool-call conversion
10. a078b4b2 — session-based prompt_cache_key routing (Codex Chat bridge)
11. 650905af — harden Responses + Anthropic protocol bridges
12. 51d6c458 — route native ChatGPT sessions through proxy takeover
13. f2c6d48e — skip reachability probes for official OAuth entries
14. f15184ed — expose official routing, restore built-in provider
15. af58740b — align Codex OAuth client identity
16. ac52c851 — infer image capability for generated catalogs, resync takeover on save

## Log

- S2-progress.md CREATED — checkpoint log initialized before any code work.
- b3e5e32c ADAPTED — Claude subagent model config. Rust: `ModelMapping.subagent_model` + preserve-before-default-fallback branch + 2 upstream tests; `CLAUDE_MODEL_OVERRIDE_ENV_KEYS` 9→10; takeover pushes/clears `CLAUDE_CODE_SUBAGENT_MODEL`; `extract_claude_common_config` ENV_EXCLUDES gains the key (kept BOTH sides — fork's `CLAUDE_CODE_MAX_CONTEXT_TOKENS`/`CLAUDE_CODE_AUTO_COMPACT_WINDOW` from S1 + upstream's subagent key).
  * EXCLUDED `ModelMapping.fable_model` — the fork never ported upstream's Fable-tier commit (no fable branch in `map_model`, no fable in `CLAUDE_MODEL_OVERRIDE_ENV_KEYS`). DEFERRED, see report.
  * EXCLUDED upstream test `extract_claude_common_config_strips_all_credentials_keeps_shareable` — it asserts generic credential scrubbing (OPENROUTER/GOOGLE/OPENAI/GEMINI/AWS_* + top-level apiKey) that the fork's `extract_claude_common_config` does NOT implement. Real gap, DEFERRED. Instead added `CLAUDE_CODE_SUBAGENT_MODEL` to the fork's own `extract_claude_common_config_excludes_model_display_names` test.
  * proxy.rs hot-switch test: took fork's env fixtures + upstream's subagent lines only (upstream's display-name fixtures belong to an unported commit); rewrote the new assertion from upstream's `live_env` to the fork's `live.get("env")`.
  * FE ADAPTED: upstream replaced the per-role blocks with a `modelRoleRows` table (fable + display names + 1M checkbox) — that whole UI belongs to unported commits, so it was DROPPED and a plain "子代理模型" field was added after Opus in the fork's existing layout. `subagentModel` threaded through ClaudeFormFieldsProps → ProviderForm → useModelState; quick-set ("一键设置") and its disabled-guard now include subagent.
  * useModelState: upstream's exported `ClaudeModelEnvField` + `hasClaudeOneMMarker`/`stripClaudeOneMMarker`/`setClaudeOneMMarker` helpers DROPPED (fork has no 1M/display-name UI); subagent added to parse/init/sync-effect/handleModelChange/return.
  * i18n: fork has 3 locales; `src/i18n/locales/zh-TW.json` stays DELETED. Upstream's `modelRoleSubagent`/`modelNoDisplayName` keys belong to the dropped table UI — instead added `providerForm.claudeCodeSubagentModel` + `providerForm.subagentModelPlaceholder` to en/ja/zh.
  * tests: upstream's `tests/hooks/useModelState.test.tsx` (new file, fork never had it) rewritten to only the 2 cases that apply to the fork's hook (hydrate + write/clear subagent). `ClaudeFormFields.test.tsx` quick-set case ported minus the `defaultSonnetModelName` override.
  * GATES: `cargo check --all-targets` PASS; `tsc --noEmit` PASS; vitest (useModelState + ClaudeFormFields) 7 PASS; `cargo test --lib -- model_mapper:: extract_claude_common_config subagent` 34 PASS.
- 3538b392 EXCLUDED — "1M checkbox on the fallback model field" is a pure UI affordance on top of upstream's Claude model-role table (`hasClaudeOneMMarker`/`stripClaudeOneMMarker`/`setClaudeOneMMarker` + `renderModelInput`'s onChange-override param + `<Checkbox>` column). The fork's FE has ZERO 1M-marker support (`grep -rn "\[1M\]|OneM" src/` → 0 hits) because it never ported that redesign, so there is nothing to attach the checkbox to. Backend behaviour is unaffected: `strip_one_m_suffix_for_upstream` still handles a manually typed `model[1M]`. DEFERRED together with the fable/display-name/1M table gap.
- 95c917b3 ADAPTED — Zhipu team plan quota. Ported the Zhipu-team half in full; EXCLUDED the Volcengine (火山方舟) AK/SK half that came bundled (fork never ported that feature — same exclusion S1 made for 2df2212c).
  * `services/coding_plan.rs`: upstream's `coding_plan_not_found` + `ZHIPU_TEAM_QUOTA_URL` + `query_zhipu_team`/`query_zhipu_team_at` taken, but using the fork's `http_client::get_guarded()` (SSRF guard) instead of upstream's `get()`. `get_coding_plan_quota` signature extended to 5 params (base_url, api_key, coding_plan_provider, team_organization_id, team_project_id) — upstream's 7 minus access_key_id/secret_access_key.
  * Test module: kept the fork's import list + `query_zhipu_team_at`; ported ONLY the 2 zhipu-team tests (`zhipu_team_missing_creds_returns_not_found`, `zhipu_team_request_carries_type2_and_org_project_headers`), adapted to 5-arg calls and the fork's `no_proxy_for_loopback()` helper name. EXCLUDED upstream's volcengine/AFP tests + its newer zenmux tests.
  * `commands/provider.rs`: BOTH upstream blocks dropped (`resolve_native_credentials`/`resolve_coding_plan_credentials`/`query_provider_usage_inner` + `import_claude_desktop_tests` + `native_query_credentials_tests`) — the fork replaced that path with `query_usage_with_templates(.., ssrf_guard)` (S1 precedent) and dropped Claude Desktop scope.
  * `services/provider/usage.rs` (fork-only file): added `CodingPlanRouting` + `coding_plan_routing(&UsageScript)` next to `resolve_coding_plan_credentials`, threaded through BOTH call sites (saved-query path and the test path, where it reads the in-flight `test_script`).
  * `web_api/handlers/subscription.rs`: `BalanceQuery` gains the 3 optional camelCase fields so the web route mirrors the widened Tauri command (`check:web-routes` still green — path/method unchanged).
  * `provider.rs` UsageScript: only `team_organization_id`/`team_project_id` added (no AK/SK); `deeplink/provider.rs` + 2 test literals updated.
  * FE: `codingPlanProviders.ts` id union gains `zhipu_team` (NOT `volcengine`) + the entry; `types.ts` + `subscription.ts` gain the team fields (no AK/SK); UsageScriptModal gains the zhipu-team credentials block (org/project ID inputs + console link) and preserves those fields on template switch. `ExternalLink` added to the lucide import.
  * FE test path ADAPTED: upstream rewrote the whole TOKEN_PLAN test branch to call `subscriptionApi.getCodingPlanQuota` directly; the fork's branch goes through `usageApi.testScript`, which reads the SAVED script and therefore cannot see just-typed org/project IDs. Kept the fork's path for every other provider and routed ONLY `zhipu_team` through `getCodingPlanQuota` with the in-modal values, plus a local `codingPlanQuotaToUsageResult` mirroring the backend's non-USD branch.
  * GATES: `cargo check --all-targets` PASS; `tsc --noEmit` PASS; `cargo test --lib -- coding_plan::` 26 PASS (incl. both new team tests); `check-web-route-coverage` missing 0 / methodMismatch 0 / parityFallback 0; `check-locale-parity` in parity (2363 keys).
- 99e11e08 **BLOCKED / NOT PORTED** — native Anthropic Messages protocol as Codex upstream (5733 insertions). Cherry-pick aborted and fully reverted (verified: `git diff --cached HEAD` shows only the b3e5e32c + 95c917b3 work; the 3 new upstream modules deleted; hyper_client.rs / thinking_optimizer.rs / providerConfigUtils.test.ts / useProviderActions.test.tsx restored from HEAD; no `codex_anthropic`/`impersonateClaudeCode` symbol anywhere; `cargo check --all-targets` PASS).
  * ROOT CAUSE (a prerequisite gap, not a conflict): **the fork never ported upstream's Codex Chat bridge**, which already existed at the fork's own sync base v3.16.5. `git ls-tree v3.16.5 src-tauri/src/proxy/providers/` has `codex_chat_common.rs`, `codex_chat_history.rs`, `streaming_codex_chat.rs`, `transform_codex_chat.rs`; the fork has NONE of them, and its `codex.rs` is a 345-line passthrough adapter with no routing layer (upstream's is ~1000 lines). The fork even documents this in `src-tauri/src/proxy/handlers.rs:811-814` and `:1347` ("Codex Chat 路由栈，fork 暂未移植").
  * Consequence: the whole `codex.rs` hunk is ONE conflict region with an EMPTY HEAD side (lines 23-479), and `streaming_codex_chat.rs` / `transform_codex_chat.rs` came through as modify/delete. Landing this commit means first back-porting the entire v3.16.5 Codex Chat bridge (4-5 modules plus forwarder / handlers / codex_config / ProviderMeta / CodexApiFormat wiring), then layering the Anthropic bridge (transform_codex_anthropic 2443 lines + streaming_codex_anthropic 1166 + codex_responses_sse 399) on top — a separate batch's worth of work in the highest-risk part of the proxy, not an S2 conflict resolution. Escalating to the team lead rather than half-landing a request path.
  * Knock-on: #10 `a078b4b2` (prompt_cache_key routing FOR the Codex Chat bridge) is blocked by the same gap; #11 `650905af` is only partially portable (its `streaming_codex_anthropic`/`transform_codex_anthropic` hunks have no target; its `streaming_responses`/`transform_responses`/`forwarder` hunks do).
- 50270d5e ADAPTED — exclude Fable model env from Claude common config (#4272). **S1's deferral is now CLOSED**: all three keys are stripped — `ANTHROPIC_DEFAULT_FABLE_MODEL`, `ANTHROPIC_DEFAULT_FABLE_MODEL_NAME` (this commit) and `CLAUDE_CODE_SUBAGENT_MODEL` (b3e5e32c) are in `extract_claude_common_config`'s ENV_EXCLUDES; `CLAUDE_MODEL_OVERRIDE_ENV_KEYS` is now `[&str; 12]`.
  * Upstream's `extract_claude_common_config_strips_fable_model_env_keys` regression test ported verbatim and PASSES. The other test in that conflict region (`..._strips_all_credentials_keeps_shareable`) stayed EXCLUDED for the same reason as in b3e5e32c.
  * `services/proxy.rs`: `CLAUDE_TAKEOVER_FABLE_MODEL` + the fable `push_claude_takeover_role_fields` call ported.
  * EXTRA (mine, flagged for review): the fork's `model_mapper` had NO fable tier, so the newly-ported takeover write would have produced a live `ANTHROPIC_DEFAULT_FABLE_MODEL`/`_NAME` that the proxy could not honor (requests would silently fall through to `default_model` while the /model menu showed the provider's fable name). Added `ModelMapping.fable_model` + the fable branch with upstream's fable→opus downgrade, using the fork's word-boundary `contains_model_tier` (its L28 fix) instead of upstream's bare `contains`. Ported upstream's 4 fable mapper tests — all pass.
  * FIXED A BUG I INTRODUCED IN b3e5e32c: the hot-switch test assertion I ported from upstream (`subagent model should follow the target provider`) is WRONG for this fork. The fork's hot switch calls `cleanup_claude_model_overrides_in_live`, which removes every `CLAUDE_MODEL_OVERRIDE_ENV_KEYS` entry after the takeover sync ("避免接管开启后切换供应商仍使用旧模型"), so subagent/fable pins are cleared like `ANTHROPIC_MODEL`. Assertion inverted to `is_none()` with a comment recording the upstream divergence. Caught by the full lib run, NOT by the filtered run I did for b3e5e32c — lesson: run `cargo test --lib` unfiltered per commit.
  * GATES: `cargo test --lib` **1554 passed / 0 failed**.
- ded0b63a ADAPTED — missing provider keys + tool schema types (#5069). FE half (`useApiKeyState.ts` createIfMissing/shouldShow rules + upstream's new `tests/hooks/useApiKeyState.test.tsx`) and `transform_responses.rs` auto-merged clean.
  * `transform.rs` `clean_schema` conflict: upstream rewrote it as `clean_schema_inner(schema, is_root)` on top of the OLD narrow recursion (`properties` + `items` only), while the fork has the much broader M30 version (recurses `$defs`/`definitions`/`patternProperties`/`dependentSchemas`/`anyOf`/`oneOf`/`allOf`/`prefixItems`/object-form `additionalProperties`/`propertyNames`/`not`/`if`/`then`/`else`/`contains`/`additionalItems`/`unevaluatedItems`). Kept the fork's full traversal and layered upstream's root `type: "object"` + empty `properties` injection on top; `clean_schema_child` now recurses via `clean_schema_inner(.., false)` so the root-only injection does NOT leak into nested sub-schemas.
  * GATES: `cargo test --lib -- schema` 39 PASS — all 8 fork M30 tests plus upstream's 5 new ones (`test_clean_schema_only_defaults_root_to_object`, `*_defaults_empty_tool_schema`, `*_defaults_missing_tool_schema_type` for both chat and responses). `tsc --noEmit` PASS; `vitest tests/hooks/useApiKeyState.test.tsx` 2 PASS.
- c6197ae3 ADAPTED — single auth placeholder on managed Claude takeover (#4919). Upstream's insert logic taken wholesale.
  * PREREQUISITE ALSO PORTED: the fork's `ClaudeTakeoverAuthPolicy::ManagedAccount` was a unit variant that unconditionally inserted only `ANTHROPIC_API_KEY` — it never had upstream's #3784 `keep_auth_token` fix (Codex-managed providers must keep `ANTHROPIC_AUTH_TOKEN` or Claude Code shows a login prompt). Added `ManagedAccount { keep_auth_token: bool }` with `keep_auth_token: !provider.is_github_copilot()` (both helpers already exist on `Provider`), which is what c6197ae3's `if keep_auth_token {…} else {…}` needs to compile. Net effect: Codex-managed → AUTH_TOKEN only; Copilot → API_KEY only; never both.
  * Test conflict had an EMPTY HEAD side: took upstream's whole block — corrected assertions on the fork's existing `managed_account_claude_takeover_sources_codex_models_from_provider` plus 3 upstream tests the fork lacked (`..._codex_injects_auth_token_without_preexisting_key`, `..._codex_by_base_url_keeps_auth_token` from #3784, and this commit's `..._codex_from_third_party_keeps_single_auth_key`). The fork's copilot test tail (API_KEY placeholder / AUTH_TOKEN None) still closes the copilot case correctly.
  * GATES: `cargo test --lib` **1563 passed / 0 failed** (+9 vs the previous run).
- 7479d10d ADAPTED (partial) — Codex default model field. **Convergent**: the fork had ALREADY implemented this field independently, under different names (`codexModelName` / `handleCodexModelNameChange` / `CodexFormFields.modelName`), with the same bidirectional TOML sync. Kept the fork's naming (upstream's rename to `codexModel` would have churned every consumer for nothing) and ported the parts the fork lacked:
  * **TOML escaping (the security half)** — `setCodexModelName` and `setCodexBaseUrl` now emit `tomlBasicString(...)` instead of bare interpolation. `/models` ids are remote data; an id containing `"` + newline could previously inject config.toml lines (e.g. a forged `[mcp_servers.*] command = …`). New strict matchers `TOML_MODEL_DOUBLE_QUOTED_PATTERN` / `TOML_MODEL_SINGLE_QUOTED_PATTERN` + `unescapeTomlBasicString` + `findTopLevelModelLineIndex` replace `TOML_MODEL_PATTERN` so escaped output round-trips; the fork's `TOML_MODEL_REPLACE_PATTERN` inline-comment preservation was KEPT, now substituting the escaped value.
  * **Control-character stripping** in `handleCodexModelNameChange`.
  * **/models staleness guard** — `fetchModelsSeqRef` + an effect that bumps the sequence and clears `fetchedModels` whenever the request identity changes. The fork's identity is `(codexBaseUrl, codexApiKey, isFullUrl)`; upstream also keys on `customUserAgent`, which does not exist in the fork's `CodexFormFields`. Note the seq-guard *checks* (`if (seq !== fetchModelsSeqRef.current) return;`) arrived via auto-merge, so without this the file would not have compiled.
  * `providerConfigUtils.ts` was resolved by hand (`git checkout --ours` then re-applying upstream's 6 hunks): the 3-way merge mis-aligned badly — one conflict had an empty HEAD side spanning 396 lines that re-inserted functions the fork already has elsewhere. Upstream's 11 new tests ported verbatim; `providerConfigUtils.test.ts` 13 PASS (incl. `escapes hostile model ids instead of injecting TOML lines`).
  * EXCLUDED: the model-catalog editor UI (`catalogModels`/`onCatalogModelsChange`/`CodexCatalogRow`, `normalizeCodexCatalogModelsForSave`, the save-time "backfill row 0 into top-level model" rule) and the anthropic-format/reasoning props — all belong to upstream features the fork never ported. The fork's `ProviderForm` save path is also restructured (M40 `assembleSettingsConfig`), so upstream's save block does not apply. `src/i18n/locales/zh-TW.json` stays DELETED; upstream's new locale keys were dropped with the UI they belong to.
  * GATES: `tsc --noEmit` PASS; `check-locale-parity` in parity; `vitest run` (full FE suite) **122 files / 655 tests PASS**.
- 27ce0a51 ADAPTED — harden Responses reasoning + tool-call conversion. The BULK auto-merged clean: new `proxy/providers/reasoning_bridge.rs` (+131), `streaming_responses.rs` (+522: stable item-id/output-index tracking, dedicated keyless-legacy fallback block, tool-arg recovery from done events, protocol-order block closing, official reasoning-text event vocabulary), `transform_responses.rs` (+270: encrypted reasoning round-trip through bridge-owned thinking blocks, orphaned reasoning-only history discarded, empty/incomplete non-streaming tool args normalized, malformed completed calls rejected).
  * Single conflict in `handlers.rs`, in the usage-logging hunk. Upstream's `ClaudeUsageLog` / `prepare_claude_usage_log` / `write_claude_usage_log` / `spawn_claude_usage_log` helpers auto-merged in but did not compile against the fork, which lacks `RequestContext.outbound_model`, `TokenUsage::has_billable_tokens`, and whose `log_usage` takes 10 args (no `outbound_model`, no `session_id`) — all from upstream commits this fork never ported. Adapted the helpers to the fork's shapes (dropped `outbound_model`/`session_id`/`app_type` fields, kept the fork's `"unknown"` model fallback and its no-billable-filter semantics) and replaced the fork's inline success-path logging with `spawn_claude_usage_log`.
  * NET BEHAVIOR GAIN (this commit's point): on a post-upstream conversion failure the tokens already consumed are now persisted before the terminal error is returned (`raw_usage_response` built from `transform_responses::build_anthropic_usage_from_responses`, which arrived with the auto-merge). Side effect: that path now honours `usage_logging_enabled(state)`, which the fork's inline version did not check even though its sibling paths do (handlers.rs:328/452) — upstream-first, treated as a fix.
  * WEB BUILD: no `#[path]` shim needed for `reasoning_bridge` — `examples/web_proxy.rs` maps `providers/mod.rs` as a whole (unlike `services/`, which is enumerated file-by-file and needed the sql_helpers shim in S1). Verified with the web `cargo check`.
  * GATES: `cargo check --all-targets` PASS; `cargo check --no-default-features --features web-server --example server` PASS; `cargo test --lib` **1572 passed / 0 failed** (+9).
- a078b4b2 **EXCLUDED (blocked)** — session-based prompt_cache_key routing FOR the Codex Chat bridge. Every hunk hangs off the un-ported bridge: `forwarder.rs` edits sit inside the `if codex_responses_to_chat` arm and call `self.codex_chat_history.enrich_request` + `transform_codex_chat::responses_to_chat_completions_with_reasoning` + the new `inject_codex_chat_prompt_cache_key`; the `codex.rs` conflict is again an EMPTY-HEAD region re-adding `codex_provider_uses_chat_completions`. Same root cause as 99e11e08. Cherry-pick reverted cleanly (unmerged 0; no `inject_codex_chat_prompt_cache_key`/`codexChatPromptCacheKey` symbol added — the `prompt_cache_key` hits that remain are the fork's own pre-existing `transform_responses.rs` injection and `ProviderMeta.promptCacheKey`; `cargo check --all-targets` PASS).
- 650905af ADAPTED (PARTIAL) — harden Responses and Anthropic protocol bridges. Ported the **Responses** half; EXCLUDED the **Anthropic-bridge** half (no target modules — same 99e11e08 gap).
  * PORTED (auto-merged clean): `streaming_responses.rs` and `transform_responses.rs` — both are fork-resident and used by the Claude→Responses path.
  * PORTED (hand-merged from `forwarder.rs`): `validate_responses_success_response` + `validate_responses_stream_start` methods, the free helpers `responses_error_envelope_message` / `inspect_responses_json_document` / `inspect_responses_start_event`, and the call site — adapted to the fork's 2-tuple `(response, resolved_claude_api_format)` return (upstream's is a 3-tuple with the unported `outbound_model`) and to `let mut response`. Real capability gain: a Claude→Responses gateway that returns a semantic failure inside an HTTP 2xx body (or a `response.failed`/`error` before any output on SSE) is now detected INSIDE the retry loop, so the request can fail over instead of surfacing a dead response.
  * Also ported `ProxyResponse::is_json()` into `hyper_client.rs` (upstream helper the fork lacked; the new call site needs it) and added `use bytes::Bytes`.
  * EXCLUDED: `streaming_codex_anthropic.rs` + `transform_codex_anthropic.rs` (modify/delete — files do not exist), `validate_codex_anthropic_success_response`, `codex_anthropic_error_envelope_message`, `codex_anthropic_cache_config`, `rewrite_codex_responses_endpoint_to_chat/_to_anthropic`, `prepend_claude_code_system_prompt`, `is_codex_client_fingerprint_header`, and their 8 tests — all Codex-bridge-only.
  * PORTED upstream's 3 new Responses tests (`responses_2xx_failure_is_detected_for_failover`, `responses_stream_start_semantic_failure_is_retryable`, `responses_stream_start_accepts_unlabelled_whole_json`) — all pass.
  * GATES: `cargo check --all-targets` PASS; `cargo test --lib` **1590 passed / 0 failed** (+18).
- 51d6c458 **EXCLUDED (blocked)** — route native ChatGPT sessions through proxy takeover (835 insertions, 11 files; 597 lines in `services/proxy.rs` alone). Cherry-pick reverted cleanly (unmerged 0; no `CHATGPT_CODEX_BASE_URL` / `is_codex_official_provider` / `CC_SWITCH_CODEX_OFFICIAL_PROXY_PROVIDER_ID` / `official_provider_supports_proxy_takeover` symbol left; `cargo test --lib` back to 1590 PASS). Three independent blockers, each on the code that rewrites the user's real `~/.codex/config.toml` + `auth.json`:
  1. **Missing prerequisite**: the fork has no `sync_codex_live_from_provider_while_proxy_active` at all (upstream has had it since before 51d6c458^ — the fork only ported the Claude twin). Conflict 1 is misaligned onto the fork's `set_runtime_ctx` as a result.
  2. **Web-fork structural divergence**: upstream's new official-takeover gate is added inside `self.app_handle.read().await` + `handle.emit(...)`; the fork replaced that whole path with `runtime_ctx` + `UiEventSink::emit_json` for web mode. Every such site needs hand-reconciliation.
  3. **Write-path refactor**: it replaces `update_toml_base_url` with `apply_codex_takeover_fields_for_provider` + `apply_codex_takeover_auth_placeholder`, renames `preserve_codex_oauth_auth_in_backup`, and rewrites how the Codex live config is projected — on top of the fork's own atomic/transactional write fixes. 10 conflicts in `services/proxy.rs` plus `codex_config.rs` (+226), `forwarder.rs` (+98), `providers_seed.rs`, `database/mod.rs`, `services/provider/mod.rs`.
  Prerequisites that DO exist in the fork: `CODEX_OFFICIAL_PROVIDER_ID`, `CodexCatalogToolProfile::NativeResponses`, `require_current_provider_for_app`, `preserve_codex_mcp_servers_from_existing_config`, `write_codex_takeover_live_for_provider` — so this is portable as its own batch, just not as an S2 conflict resolution. Recommend scheduling it together with f15184ed (its commands/FE counterpart).
- f2c6d48e ADAPTED (PARTIAL) — skip reachability probes for official OAuth entries.
  * PORTED: `commands/stream_check.rs` — `stream_check_all_providers` now `continue`s on `category == "official"`, so a batch check never turns an official provider's runtime adapter defaults into an unauthenticated probe against a first-party endpoint. Auto-merged clean; this is the half that matters for the fork.
  * EXCLUDED: the `services/stream_check.rs` half. Upstream guards `resolve_base_url`, but **the fork has no `resolve_base_url` and no `probe_reachability`** — it never ported upstream's "reachability probe" refactor and still does real stream checks via `resolve_claude_stream_url` / `resolve_codex_stream_urls`. Both conflicts in that file were pure misalignment (git aligned upstream's re-emitted function and its 2 new assertions against unrelated fork test bodies); took HEAD for both.
  * GATES: `cargo test --lib` 1590 PASS (unchanged — the ported hunk is behavioural, untested upstream too).
- af58740b DONE — align Codex OAuth client identity. Applied CLEAN (no conflicts). `CODEX_OAUTH_ORIGINATOR = "codex_cli_rs"` + `CODEX_OAUTH_CLIENT_VERSION = "0.144.1"` now sent as a pair on the CodexOAuth takeover path (`providers/claude.rs`), replacing the bare `originator: cc-switch`; `auth.rs` doc updated. Fixes the ChatGPT cohort routing that made gpt-5.6-luna 404. `cargo test --lib` 1590 PASS.
- ac52c851 ADAPTED — infer image capability for generated catalogs + resync takeover live on save. The headline fix (mapped GPT models rejected with "model does not support image inputs") is fully ported.
  * NEW MODULE `src-tauri/src/model_capabilities.rs` taken from upstream, with one fork adaptation: `normalize_model_id` referenced `crate::claude_desktop_config::ONE_M_CONTEXT_MARKER` (the fork dropped Claude Desktop scope) → repointed at the fork's `crate::proxy::model_mapper::ONE_M_CONTEXT_MARKER` (same `"[1m]"` value). **WEB BUILD: added the `#[path = "../src/model_capabilities.rs"]` shim to `examples/server.rs`** — top-level modules are enumerated there file-by-file (the S1 sql_helpers lesson); without it only the web gate would have failed.
  * Verified upstream's `CONFIRMED_TAILS` registry is a strict SUPERSET of the fork's `EXACT_TAILS` + `TAIL_PREFIXES` (upstream enumerates the prefix variants: minimax-m2.7*, qwen3-coder-*, step-3.5-flash*, plus longcat-2.0). No fork-local model classification lost.
  * `codex_config.rs`: hand-applied (8 conflicts, several pure misalignment incl. an 848-line empty-HEAD test block). Added `codex_catalog_input_modalities`; `input_modalities` is now written for ALL tool profiles (was NativeResponses-only) with unknown models failing OPEN to `["text","image"]`; reverse-import collapses modalities that equal the inference.
  * `media_sanitizer.rs`: hand-applied (taking the upstream side wholesale had dropped `replace_images_in_content_with_text_type`, which the fork still calls). Deleted the 8 functions that moved into `model_capabilities`, delegated to `image_input_capability_from_settings`, renamed `known_text_only_model` → `confirmed_text_only_model` (test-only import).
  * TEST SEMANTICS UPDATED: the fork's `simplified_catalog_exposes_codex_model_metadata_for_models_endpoint` asserted `inputModalities == ["text","image"]`; under the new collapse rule that value is the inference for an unregistered model and is now correctly omitted. Assertion flipped to `is_none()` with a comment, and upstream's dedicated `build_simplified_catalog_squashes_inferred_modalities_and_keeps_overrides` ported to cover the keep-overrides side.
  * **WROTE `sync_codex_live_from_provider_while_proxy_active`** (fork-idiom, ~40 lines) — the auto-merged `services/provider/mod.rs` resync branch needs it and the fork had no Codex twin of `sync_claude_live_from_provider_while_proxy_active`. Mirrors the Claude twin: `build_effective_settings_with_common_config` → `preserve_codex_mcp_servers_from_existing_config` → placeholder auth + `update_toml_base_url` → `write_codex_takeover_live_for_provider` (which projects/clears `model_catalog_json` via `prepare_codex_live_config_text_with_optional_catalog`). Deliberately expressed with the fork's existing helpers rather than 51d6c458's `apply_codex_takeover_fields_for_provider` refactor, so it does NOT pull in that blocked commit. FLAG FOR REVIEW: it writes the user's real `~/.codex/config.toml`.
  * EXCLUDED: upstream's 2 new `services/provider/mod.rs` integration tests (one needs 51d6c458's takeover projection, the other is Claude Desktop scope); `zh-TW.json` stays deleted; CHANGELOG.md left at HEAD.
  * GATES: `cargo check --all-targets` PASS; web `cargo check --no-default-features --features web-server --example server` PASS; `cargo test --lib` **1596 passed / 0 failed**; `tsc --noEmit` PASS; `vitest run` 122 files / 655 tests PASS; `check-locale-parity` in parity; `check-web-routes` missing 0 / methodMismatch 0 / parityFallback 0.
  * NOTE: `database::backup::tests::sql_import_holds_main_lock_across_safety_backup_and_replace` failed once under full-suite load and passes in isolation and on re-run — pre-existing lock-timing flake, not caused by this work.
- f15184ed **EXCLUDED (blocked with 51d6c458)** — expose official routing + restore the built-in provider. It is the UI/commands counterpart of 51d6c458: `supportsOfficialProxyTakeover()` gates a "不支持路由" badge and unblocks selecting the built-in official Codex provider while takeover is active. Landing it WITHOUT 51d6c458 would advertise a capability the backend cannot serve — the fork's `ProviderCard` currently blocks official-under-takeover on purpose (`isProxyTakeover && provider.category === "official"`), and there is no `official_provider_supports_proxy_takeover` in the fork. Also adds a new Tauri command `ensure_codex_official_provider`, which under the fork's rules needs a `web-commands.ts` route + web handler. Reverted cleanly (unmerged 0; `providerCapabilities.ts` deleted; no `supportsOfficialProxyTakeover`/`ensureCodexOfficialProvider` symbol left).

## FINAL GATE RUN

- `cargo fmt --check` PASS (needed one `cargo fmt` after the media_sanitizer function deletions)
- `cargo clippy --all-targets -- -D warnings` PASS — caught a duplicated `#[test]` and a lost `#[test]` from my forwarder test splice; both fixed
- `cargo clippy --no-default-features --features web-server --example server -- -D warnings -A dead_code -A clippy::duplicate_mod` PASS
- `cargo check --no-default-features --features web-server --example server` PASS
- `cargo test --lib` **1596 passed / 0 failed / 2 ignored**
- `cargo test` (full desktop) — all suites pass EXCEPT `tests/provider_commands.rs`. Under `--test-threads=1` exactly ONE test fails: `switch_provider_updates_codex_live_and_state` ("live file should contain provider's original config"). **Verified pre-existing**: stashed all S2 work and ran the same serialized command on clean HEAD — identical single failure. The 2 extra failures seen in the default parallel run are cross-test `$HOME`/global-settings interference (these tests are not `#[serial]`) and appear on clean HEAD too.
- `npx tsc --noEmit` PASS
- `npm run format:check` PASS (the repo gate is scoped to `src/**`; bare `npx prettier --check .` also flags pre-existing `.agents/`, `tests/`, config files untouched by S2)
- `npx vitest run` **122 files / 655 tests PASS**
- `node scripts/check-web-route-coverage.mjs` missing 0 / methodMismatch 0 / parityFallback 0
- `node scripts/check-locale-parity.mjs` in parity
- Integration suite NOT run (final S8 gate, per brief)

## END STATE

- `git ls-files -u` empty; no `CHERRY_PICK_HEAD`/`MERGE_HEAD`; zero conflict markers in staged content
- All work staged, NO commit made
