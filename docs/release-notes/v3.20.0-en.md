# CC Switch v3.20.0

> Synced product-upstream `farion1231/cc-switch` `v3.19.2..v3.20.0` into this Web-first fork via selective port across seven themed batches (S1–S7). The standalone Web-server deployment model, dual-runtime parity, the no-auth posture, the disabled desktop updater, and the existing Web hardening are all preserved. Three large new features (Pi native coding agent, Codex Alpha WebSearch, managed OAuth accounts) are split into dedicated subtasks and land separately.

**[中文版 →](v3.20.0-zh.md) | [日本語版 →](v3.20.0-ja.md)**

---

## Highlights

- **S1 docs/presets/i18n**: the PPIO, JieKou AI and XycAi partner presets registered across all supported apps with brand icons and, where supported, endpoint fallbacks; the RunAPI preset migrated to `runapi.host` (with `runapi.co` kept as a fallback endpoint); the legacy partner ⭐ badge removed from the main-panel provider cards (partner stars now live only in the preset selector); the `xycai` partner promotion surfaced in every locale; the version bumped to 3.20.0.
- **S2 security/data-integrity**: env-check preflight hang fix; Codex ChatGPT login no longer clobbered (prerequisite for the managed-OAuth subtask); Kimi thinking injection stop; zhipu `CREDIT_LIMIT` quota; routing animate activation; sync/restore consistency (transactional restore, prepare-once INSERT); backup SQL fidelity (multi-row `VALUES` batching, trigger/index ordering). The fork's canonical-schema allow-list, backup/replace lock boundary, 2s JS deadline, 16 MiB heap / 256 KiB stack caps, and SQLite authorizer strictness were all preserved.
- **S3 pricing/usage**: DeepSeek V4 peak + Gemini 3.7 Flash seeds; DeepSeek catalog mirror extraction; Grok 4.5 cached + Grok 4.6 + DeepSeek alias; multi-year trend tooltip; DeepSeek cache-hit tokens. FE-preset ↔ Rust-seed consistency gate passed (no `cost=$0`).
- **S4 codex/provider**: drop Goal mode toggle; restore 1M context toggle; IME-safe provider fields (new shared `ImeSafeInput` adopted across Hermes/OpenClaw/OpenCode forms); cancel stale device login; Grok Build Codex copy; grokbuild form align; macOS IME corruption fix. The `0455a92c` multiple-follow-login and `897ca892` OAuth-usage work were split (frontend increments landed in the parent; Rust managed-codex transactions transfer to the managed-OAuth subtask). The `40cac1a6` per-model reasoning levels were split (catalog data layer landed; transform/UI layer deferred).
- **S5 UI/refactor**: Hermes/OpenCode/OpenClaw form hierarchy alignment; checkbox styles unified (Radix → native `<input>`, preserving `onCheckedChange`/`checked` contracts); OpenClaw model editor; model-dropdown consolidation; fuzzy model dropdown (created the shared `ModelDropdown.tsx` the fork never carried); Claude advanced-options alignment; reduced editor empty space; GrokBuild glass container; skills `check_updates` now reports a missing SSOT dir as an update.
- **S6 Windows**: startup FOUC eliminated (anti-FOUC inline script + themed window background + CSP SHA-256); Windows CLI detection from registry PATH + standalone installer dirs (adapted to the fork's `services/tool_version.rs`); WiX Handlebars backslash escape; WSL atomic-replace fallback (fork's 2s deadline + heap/stack caps preserved).
- **S7 CI**: skip frontend/backend checks when a PR touches neither area (adapted to the fork's 3-job `frontend`/`backend`/`web-server` structure with a `changes` job using `dorny/paths-filter`, SHA-pinned).

## Fork-Specific Deviations

- The `zh-TW` locale and `README_DE` are not restored; upstream hunks touching them were dropped as no-ops.
- The `claudeDesktopProviderPresets` surface and the desktop-only sponsor banner tables in the READMEs remain excluded, so upstream's desktop-preset and README-partner hunks were dropped as no-ops.
- The Codex Chat routing stack (`streaming_codex_chat.rs` / `transform_codex_chat.rs` / `codex_chat_common.rs` / `transform_codex_anthropic.rs`) remains an intentional carry-forward deferral. Upstream commits touching only that stack were skipped as proven-inapplicable: `3f75bbdf` (StepFun effort), `46f19a15` (DeepSeek cache-hit tokens, transform layer), `d01eab97` (OpenCode Zen reasoning), `6a7da87c` (grokbuild input token details), and the transform/UI layer of `40cac1a6`. Restoring the stack requires porting the full ~5.7k+ LOC dependency base first.
- The `36ed280d` i18n labeler glob fix was skipped because the fork has no `.github/labeler.yml` (config) nor `workflows/labeler.yml` (workflow). Its intent — correctly recognizing `src/i18n/locales/**` as an i18n path — is equivalently covered by the inlined path filters in `c98cc3a9` (the `src/**` group).
- The `7e5007d5` / `619a592c` Claude Desktop form commits were skipped as proven-inapplicable (the fork has no `ClaudeDesktopProviderForm.tsx`). The `390102a2` OpenCode Go DeepSeek contextWindow commit was skipped (the fork has no "OpenCode Go" preset, and the fork's DeepSeek official preset already carries `contextWindow`).

## Out of Scope (Subtasks)

Three large new features are split into dedicated subtasks and land separately, each with its own planning, gate, and archive:

- **Pi native coding agent** (`84e75ad2` + `40d747c0` session usage) — a brand-new provider the fork does not yet carry.
- **Codex Alpha WebSearch** (`bdeaac75`) — Codex Alpha Search + Claude hosted WebSearch.
- **Managed OAuth accounts** (`a2e22f33`) — managed OAuth account selection (depends on the S2 `d2b070c9` "never clobber Codex login" fix, which has landed in the parent main body).

The parent task performs the cross-subtask integration review and unifies the version/changelog before merging to `main`.
